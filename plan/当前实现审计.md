# 当前实现审计

> 本文是继续 TODO 前的纠偏门禁。后续开发不能只看 TODO 打勾状态，还必须先核对这里的“真实实现状态”。
> 更新时间：2026-06-27。当前口径基于代码复核后的真实状态；历史 TODO 打勾只能作为完成记录线索，不能直接作为验收通过依据。

---

## 一、结论

当前项目已经不是“只有前端 Mock 页面”的状态，也不能写成“真实外部生成闭环已完成”。准确状态是：

```text
前端主线页面较完整
+ Rust / SQLite / Task / Provider / SceneRepository / FFmpeg 底座已建立
+ 生图、图生视频、合成已经有 Tauri/SQLite 层的受控闭环
+ ProviderManager、provider_models、workflow_presets、PromptSkill、结构化输出校验已落地
+ start_composition 已接 FFmpeg sidecar 检测、片段探测、concat 和转码兜底
+ Project 基础更新已补 Tauri/SQLite 闭环：`update_project` 按文档协议真实 PATCH 基础字段，并拒绝修改 lifecycle
+ 我的作品项目生命周期已补 Tauri/SQLite 闭环：归档、恢复、软删除、默认列表排除 deleted
- 当前生成 Provider 仍是 controlled fake / dummy adapter，不是外部真实供应商
- 当前本机缺少 sidecars/ffmpeg.exe 和 sidecars/ffprobe.exe，因此真实可播放 final.mp4 smoke 还未执行
- 创作资源、素材库、视频包、设定一致性、导出、诊断、增强项、发布合规和独立 workflow 多数仍是底座或受控闭环
- 历史 TODO-01 ~ TODO-12 存在全部打勾记录，但真实外部能力、发布验收、完整增强 workflow 不满足“完成才能打勾”的规则，后续必须重开缺口跟踪
- 当前仍不能写成外部真实 Provider / 真实付费生成 / 完整发布验收已完成
```

后续开发优先级应为：

```text
1. 先做全局验证：`cargo fmt --check`、`cargo check`、`cargo test`、`pnpm --dir src typecheck`、`pnpm --dir src build`。
2. 若全局验证失败，优先修复当前代码问题，并同步对应 TODO / 审计记录。
3. 若全局验证通过，再做桌面 smoke / 发布 smoke；真实外部 Provider、真实费用、真实 sidecar smoke、导出外部目录和删除文件动作仍必须单独确认。
4. 后续新增需求必须先落到新的 TODO 或补充计划，不能继续按旧“TODO-08 未完成”口径执行。
```

---

## 二、真实目录与运行命令

当前真实结构：

| 模块 | 路径 |
|---|---|
| 前端源码 | `src/src` |
| 前端包目录 | `src` |
| Tauri/Rust | `src-tauri` |
| 根脚本 | `package.json` |
| 当前执行计划 | `plan/TODO-01.md` ~ `plan/TODO-12.md` 存在历史 `【X】` 记录；真实未完成项以后续查漏 TODO / 本审计为准 |

常用验证命令：

```text
pnpm --dir src typecheck
pnpm --dir src build
pnpm tauri:dev
pnpm tauri:build
cd src-tauri
cargo fmt -- --check
cargo check
cargo test
```

历史完成记录里如果还有 `app/frontend`、`app/src-tauri`，按旧路径记录处理；当前执行必须以本节为准。

---

## 三、逐项功能核对

| 功能 / 页面 | 当前状态 | 已实现且应保留 | 需要纠偏 / 补齐 |
|---|---|---|---|
| 我的作品 | Tauri/SQLite + 浏览器 Mock adapter | 列表、搜索、进入作品、新建入口、作品卡片；2026-06-27 已补 `update_project_lifecycle`，支持归档、恢复、软删除，默认列表排除 `deleted`，Rust 单测覆盖 | TODO-09 继续核对分页、空态、错误态、任务摘要和资源消耗统计是否来自真实数据 |
| 开始创作 / 内容创作 | Tauri/SQLite 链路已补关键缺口 | `topic / paste / article`、比例、语言、段数、时长、风格提示词、创建后进入工作台；2026-06-27 已补 `inputOptions.confirmedSegments` 到 Rust `StoryboardItem` 生成和持久化；`update_project` 已按协议真实更新基础字段，不能修改 lifecycle | 长文本检测、智能切分建议、手动兜底体验、script-editor 清理仍需继续补齐 |
| script-editor | 页面文件仍存在，但不在当前路由里 | 当前路由未注册 `script-editor`，主流程不经过它 | TODO-09 必须确认它不作为主流程必经入口；若保留，只能作为后续高级编辑入口，不能重复编辑同一批旁白 |
| 工作台 | 前端页面 | 概览、步骤入口、作品信息、最新任务摘要 | Header 快捷导航、当前作品资源消耗摘要、入口层阻断和小屏体验在 TODO-09 收口 |
| 分镜 | Tauri/SQLite 生产链路已开始落地 | 表格编辑、保存、批量保存、批准、重生成、恢复、增删、拆分、合并、上下移动、进入生图前校验；Rust 侧已有 SceneRepository | 仍需在 TODO-09 收口 Inspector 辅助面板、行号跳转、删除/合并/拆分下游影响提示和最终 UI 细节 |
| 生图 | Tauri/SQLite + controlled fake Provider | 通过 ProviderManager、list_executable_media_options、ImageInputPlan 写入本地候选文件和 ImageCandidate；支持 revision、失败隔离和下游 reset | 还不是真实外部供应商；后续真实 Provider 需要显式配置、确认费用和 smoke；最终 prompt 预览在 TODO-09/08 继续补齐 |
| 视频 | Tauri/SQLite + controlled fake Provider | 读取 selectedImageId，基于 VideoInputPlan 写入 VideoSegment；支持 provider_model / workflow_preset 能力校验、revision、失败隔离 | 还不是真实外部视频供应商；不同模型输入差异继续由 VideoInputPlan 管，不允许页面按模型名写分支 |
| 合成 | FFmpeg sidecar 链路已接入，但本机真实 sidecar smoke 未完成 | `start_composition` 不再是直接报错；已接 require_ffmpeg_sidecars、ffprobe 探测、concat、转码兜底、stderr 脱敏和截断 | 当前本机缺 `sidecars/ffmpeg.exe` / `sidecars/ffprobe.exe`，所以真实可播放 `final.mp4` 需放入 sidecar 后再 smoke；导出记录在 TODO-09 |
| 创作资源 | Tauri/SQLite + 前端基础闭环 | 菜单和页面承接视频包、创作规则、素材库；Asset / AssetReference、视频包、Style / Character / Location Bible、参考图绑定和生成上下文快照已有 TODO 完成记录 | 仍需全局验证和桌面 smoke；真实外部 Provider 生成参考图仍需用户确认 |
| 模型 / 工作流 | 已从占位变为可配置基础页 | Provider 连接、密钥引用、provider_models、workflow_presets、list_executable_media_options、dry_run / real_generate 二次确认已落地 | 当前 real_generate 仍是 dummy adapter 模拟，不是外部真实调用；业务页只能读可执行选项，不能读密钥或按模型名分支 |
| 系统设置 | 部分可用 + 发布自检底座 | 主题、语言、密度、sidecar 状态入口、密钥安全、诊断、发布自检和合规清单已有 TODO 完成记录 | 仍需全局验证、发布版 smoke 和真实 sidecar 文件检查 |
| Task / Step / Attempt / Artifact | Rust 底座较完整 | SQLite 表、状态模型、pipeline、租约、幂等、attempt、重试、取消、ProgressEvent、结构化日志已落地并有测试 | 当前批量行级真实 PipelineEngine 还需随后续页面/真实执行深化；ProgressEvent 只做实时展示，DB 仍是最终状态源 |
| API adapter | 已纠偏 | `shared/api/invoke.ts` 为 `auto`，浏览器无 Tauri 桥走 Mock，桌面有桥走 Tauri | 后续每条 TODO 涉及页面时仍需做 Tauri smoke，不能把浏览器 Mock 当桌面结果 |
| Tauri command 协议 | 主线与扩展命令较完整 | Project、Task、Config、Media、Provider、FFmpeg、Export、Template、VideoPack、Style、Character、Location、Novel、LongContent、DigitalHuman、MaterialEdit、ImageSlideshow、CanvasEdit、LocalMemory 等命令已注册；`update_project` 不再是假返回，已真实写 SQLite；2026-06-27 全量 `cargo test` 通过 | 仍需桌面 smoke 验证真实运行时注册完整性 |

---

## 四、历史 TODO 标记反向核对

这次只做门禁级抽查，不重跑所有历史测试。结论：历史 `【X】` 不能继续等同“完成”。下表改按“历史标记 / 真实状态 / 必须补齐”记录：

| TODO | 历史标记 | 真实状态 | 必须补齐 |
|---|---|---|---|
| TODO-01 | 全部 `【X】` | 主线口径、前后端分层、枚举字典、主题和 i18n 规则有文档和代码支撑 | 早期完成记录里有旧路径，当前按 `src` / `src-tauri` 执行 |
| TODO-02 | 全部 `【X】` | Project、StoryboardItem、ImageCandidate、VideoSegment、CompositionTask、AppErrorDto 和前端 API/store 已定义 | 部分记录仍写 mock 输出路径，真实下载/存储已由后续 TODO 继续推进 |
| TODO-03 | 全部 `【X】` | 当前主线页面和 Mock 闭环存在，3.11 已修正“重生成覆盖旧候选”的旧策略 | Mock 完成不等于真实外部生成完成；后续必须继续区分 Mock / Tauri / Provider |
| TODO-04 | 全部 `【X】` | SQLite、核心表、StorageService、PathGuard、Keyring、SecretGuard、AppConfig、Asset 最小能力已落地 | 4.9 只是 Asset / AssetReference 最小能力，完整素材库在 TODO-08 |
| TODO-05 | 全部 `【X】` | Task、Step、Attempt、Artifact、队列、租约、取消、ProgressEvent、reset 规则已落地 | 5.10 明确写了 scene_service 当时仍是 stub；后续已在 TODO-07 推进，但不能把 ProgressEvent 当最终状态源 |
| TODO-06 | 全部 `【X】` | ProviderManager、Provider 设置、provider_models、workflow_presets、list_executable_media_options、PromptSkill、结构化输出、dry_run / real_generate 门禁已落地 | real_generate 当前是 dummy adapter 模拟，不代表真实供应商已经接入 |
| TODO-07 | 全部 `【X】` | controlled fake 生图/视频、SceneRepository、FFmpeg/ffprobe 检测、probe、concat、转码兜底、日志脱敏已落地 | 真实外部 Provider 未接入；本机缺真实 FFmpeg/FFprobe sidecar，真实可播放 final.mp4 smoke 未完成；不能写成真实外部生成闭环 |
| TODO-08 | 全部 `【X】` | 创作资源、素材库、视频包、Style / Character / Location Bible、参考图绑定、生成上下文快照、锁定与重生成保护已有完成记录 | 需要全局验证和页面 smoke 继续确认 |
| TODO-09 | 全部 `【X】` | 导出、项目包、备份恢复、诊断包、错误码、日志归档、测试体系、MVP smoke 清单、主线 UI 收口已有完成记录 | 真实导出外部目录和真实 sidecar smoke 仍需按安全规则确认 |
| TODO-10 | 全部 `【X】` | TTS、字幕、封面、模板、Chromium 渲染安全、BGM、合成增强接入已有完成记录 | 真实 TTS Provider、真实 Chromium/FFmpeg 运行结果仍需 smoke |
| TODO-11 | 全部 `【X】` | 首发平台、随包资源、首次启动初始化、sidecar 自检、发布 smoke、签名更新预留、合规清单、发布检查清单有底座和文档记录 | 真实安装包、签名、随包资源完整性和发布版 smoke 未完成，不能算发布完成 |
| TODO-12 | 全部 `【X】` | 脚本增强、小说长内容、数字人、素材成片、纯图文、画布精修、高级视频、创作规则版本化、本地记忆/RAG 底座已有完成记录 | 多数为后续能力底座，不代表真实外部 workflow 已完整生成 |

执行规则：

```text
1. 历史 `【X】` 不再作为唯一完成依据；必须同时满足代码、验证、smoke 和真实能力边界。
2. 如果发现某个 `【X】` 只有文档或底座没有代码/真实验收支撑，必须追加未完成项并继续实现。
3. 打勾前必须写清本条属于 Mock 闭环、Tauri/SQLite 闭环、controlled fake Provider 闭环，还是外部真实 Provider / FFmpeg 闭环。
```

---

## 五、文档需求到 TODO 覆盖核对

当前 `doc/功能模块` 和参考项目吸收点在 TODO 中都有落点。覆盖关系如下：

| 文档需求 / 参考吸收点 | TODO 落点 | 当前状态 |
|---|---|---|
| 内容导入、长文本检测、智能切分、手动兜底、script-editor 不做必经页 | TODO-03、TODO-09 9.11、TODO-12 长内容 | 已补 `confirmedSegments` 生成分镜并持久化；长文本检测、智能切分建议、手动兜底体验仍需按页面和后端验收继续查漏 |
| 分镜字段结构、表格主编辑、Inspector 辅助、行号跳转、下游影响提示 | TODO-03、TODO-05、TODO-07、TODO-09 9.11 | Tauri/SQLite 分镜主链路已有；Inspector 辅助面板、行号跳转、下游影响提示必须按 UI smoke 复核，不得只看历史打勾 |
| 提示词跟 StoryboardItem 走、最终 prompt 预览、创作规则管理 | TODO-06、TODO-08、TODO-09、TODO-12 | 创作规则和 prompt preview 底座已有；最终发送给模型的完整 prompt 预览需要继续用页面和 command 复核 |
| 候选图 / 候选视频保留、选择、撤回、清理、保存为素材 | TODO-03、TODO-07、TODO-08 | Tauri/SQLite + controlled fake 候选链路已有；真实外部 Provider 产物、保存素材页面 smoke 仍需单独验收 |
| 任务态、ProgressEvent、运行中状态、失败恢复、不显示 ETA | TODO-05、TODO-09 | Task/Step/Attempt 和 ProgressEvent 底座有测试；页面任务态、批量进度不显示 ETA 仍需 UI smoke 复核 |
| 资产贯穿流程、Asset / AssetReference、删除保护、导出按引用收集 | TODO-04、TODO-08、TODO-09 | 资产和引用底座已有；导出按引用收集、删除保护的页面交互和真实文件 smoke 仍需复核 |
| 视频包 / 创作规则 / 模型工作流分层 | TODO-06、TODO-08 8.1.1、TODO-12 | 分层原则已落库：视频包引用规则和可执行项，不复制第二套规则/模型正文；PackSnapshot / SkillSnapshot / hash 仍在 TODO-12 后续 |
| 模型 / 工作流配置、Provider、API 模型能力、Workflow Preset、测试记录 | TODO-06 | 已完成基础配置和 dummy 测试门禁；真实外部调用、真实费用、真实素材上传下载仍需用户确认和单独 smoke |
| 合成页只管拼接、输出信息和导出；字幕/封面/BGM 后移 | TODO-07、TODO-09、TODO-10 | 合成页 UI 已按旧设计收口到合成/输出/导出方向；真实 FFmpeg sidecar 可播放成片仍未在本机完成 smoke |
| 导出、项目包、备份、诊断、安全扫描、测试体系 | TODO-09 | Rust 层导出/备份/诊断底座已有；真实外部目录打开、实际包内容和桌面 smoke 仍需复核 |
| TTS、字幕、封面、模板、BGM | TODO-10 | 增强项底座已有；真实 TTS Provider、真实 Chromium/FFmpeg 运行结果仍需 smoke，不得算主线完成门槛 |
| 桌面打包、发布、自检、合规 | TODO-11 | 自检和发布文档底座已有；真实 Windows 安装包、签名、随包资源完整性和发布版 smoke 未完成 |
| 脚本高级能力、小说长内容、数字人、素材成片、纯图文、画布精修、高级 AI 视频、RAG | TODO-12 | 多数为后续 workflow 底座；不能写成真实外部 workflow 已完整生成 |

结论：

```text
1. TODO 在阶段级覆盖了文档所有主要需求，但历史完成标记存在过度乐观问题。
2. TODO-01 ~ TODO-12 的历史打勾不等于真实外部 Provider、真实付费调用、真实安装包和真实 sidecar smoke 已完成。
3. 当前已验证项只能按实际范围写：`cargo test`、`pnpm --dir src typecheck`、`pnpm --dir src build` 已通过；桌面 smoke、发布 smoke、真实外部 Provider smoke 未执行。
4. 如果后续新增需求，必须先判断放进新的 TODO / 补充计划，还是明确不做，不能临时塞页面。
```

---

## 六、当前下一步

当前应继续：

```text
全局验证与 smoke：
1. 继续补齐真实缺口：桌面 smoke、真实 sidecar smoke、发布 smoke、真实 Provider 接入前确认。
2. 每修一个真实断点，必须同步代码、测试、文档状态；不能只改 UI 或只改文档。
3. 在 sidecar / 浏览器 / 桌面条件满足后执行 smoke 清单。

本轮已执行验证：

```text
cargo test update_project_lifecycle_persists_and_list_excludes_deleted_by_default
cargo test update_project_persists_basic_fields_without_lifecycle
cargo check
cargo test
pnpm --dir src typecheck
pnpm --dir src build
```

结果：Rust `250 passed; 0 failed; 1 ignored`，`cargo check` 通过，前端 typecheck / build 通过。ignored 项为真实模板 sidecar 测试，需要真实 sidecar 条件。
```

继续约束：

```text
1. 不能把 controlled fake / dummy adapter 写成真实外部供应商。
2. 不能把未执行的真实 FFmpeg sidecar smoke 写成已通过。
3. 不能把页面 Mock 当桌面 Tauri 结果。
4. 如果全量验证失败，先修真实失败，再同步 TODO / 审计记录。
```

---

## 七、后续执行门禁

进入任何新 TODO 前必须做：

```text
1. 读本文，确认当前依赖是 Mock、Tauri/SQLite、controlled fake Provider，还是外部真实能力。
2. 读当前 TODO 的“问题 / 位置 / 改法 / 验收 / 验证 / 风险 / 下一步进入条件”。
3. 命中参考项目吸收点时，读 `doc/参考分析/参考项目深度机制与吸收口径.md` 对应施工卡。
4. 打勾前重新核对本条实际完成范围、验证命令和剩余 `【】`。
```

禁止：

```text
1. 不把 controlled fake / dummy adapter 写成真实外部供应商。
2. 不把本机未执行的真实 FFmpeg sidecar smoke 写成已通过。
3. 不把页面 Mock 当桌面 Tauri 结果。
4. 不为了赶 UI 绕过 Repository / Service / Tauri command。
5. 不在当前 TODO 外私自新增第二套概念，例如“模板创建”替代视频包。
```
